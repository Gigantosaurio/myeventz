# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (XAMPP MySQL)
# Por defecto XAMPP no tiene contraseña para root
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=myeventz

# JWT Configuration
# ⚠️ IMPORTANTE: Cambiar en producción por un secreto seguro
JWT_SECRET=mi_secreto_super_seguro_cambiar_en_produccion_minimo_32_caracteres_aqui
JWT_EXPIRES_IN=7d

# CORS Configuration
# URL del frontend (Vite usa 5173 por defecto)
CORS_ORIGIN=http://localhost:5173

# Upload Configuration
UPLOAD_MAX_SIZE=5242880
UPLOAD_DIR=uploads

# API Configuration
API_PREFIX=/api
